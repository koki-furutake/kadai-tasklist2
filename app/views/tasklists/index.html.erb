<h1>タスク一覧</h1>
<% if @tasklist.any? %>

  <table class="table table-striped">
    <thead>
      <tr>
        <th>id</th>
        <th>ステータス</th>
        <th>タスク</th>
      </tr>
    </thead>
    <tbody>
      <% @tasklist.each do |tasklist| %>
        <tr>
          <td><%= link_to tasklist.id,tasklist %></td>
          <td><%= tasklist.content %></td>
          <td><%= tasklist.status %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <div>
  </div>
<% end %>
<%= paginate @tasklist %>

<%= link_to "新規タスクの追加",new_tasklist_path,class: "btn btn-primary" %>